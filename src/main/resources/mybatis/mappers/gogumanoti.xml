<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="himedia.hbgoguma.mappers.GogumaNotificationMapper">
	<select id="selectAll" 
		resultType="gogumaNoti">
	SELECT * FROM goguma_notification ORDER BY nid DESC
	</select>
	
	<select id="selectOne"
		parameterType="long"
		resultType="gogumaNoti">
	SELECT * FROM goguma_notification WHERE nid=#{nid}
	</select>
	
	<insert id="insert" parameterType="gogumaNoti">
	INSERT INTO goguma_notification (receiveUid, sendUid, notiCategory, notiContent)
	VALUES(#{receiveUid}, #{sendUid}, #{notiCategory}, #{notiContent})
	</insert>
	
	<update id="update" parameterType="gogumaNoti">
	UPDATE goguma_notification SET
	receiveUid=#{receiveUid}, sendUid=#{sendUid}, notiCategory=#{notiCategory}, notiContent=#{notiContent} 
	WHERE nid=#{nid}
	</update>

	<delete id="delete" parameterType="long">
	DELETE FROM goguma_notification WHERE nid=#{nid}
	</delete>
</mapper>