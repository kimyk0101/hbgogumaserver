<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="himedia.hbgoguma.mappers.GogumaUserMapper">
	
	<select id="selectAll" 
		resultType="gogumaUser">
	SELECT * FROM goguma_user ORDER BY uid DESC
	</select>
	
	<select id="selectOne"
		parameterType="long"
		resultType="gogumaUser">
	SELECT * FROM goguma_user WHERE uid=#{uid}
	</select>
	
	<insert id="insert" parameterType="gogumaUser">
	INSERT INTO goguma_user (name, nickname, password, tel_number, email, 
							loca_gu, loca_dong, thumbnail, recommend_uid, pumpkin_point, 
							user_rate, register_date, is_deleted)
	VALUES (#{name}, #{nickname}, #{password}, #{tel_number}, #{email}, #{loca_gu}, 
			#{loca_dong}, #{thumbnail}, #{recommend_uid}, #{pumpkin_point}, #{user_rate}, 
			#{register_date}, #{is_deleted})
	</insert>
	
	<update id="update" parameterType="gogumaUser">
	UPDATE goguma_user SET
	name=#{name}, nickname=#{nickname}, password=#{password}, telNumber=#{tel_number}, 
	email=#{email}, locaGu=#{loca_gu}, locaDong=#{loca_dong}, thumbnail=#{thumbnail}, 
	recommend_uid=#{recommend_uid}, pumpkin_point=#{pumpkin_point}, userRate=#{user_rate}, 
	registerDate=#{register_date}, isDeleted=#{is_deleted}
	WHERE uid=#{uid}
	</update>

	<delete id="delete" parameterType="int">
	DELETE FROM goguma_user WHERE uid=#{uid}
	</delete>
</mapper>