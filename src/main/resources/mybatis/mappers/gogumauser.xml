<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="himedia.hbgoguma.mappers.GogumaUserMapper">
	
	<select id="selectAll" 
		resultType="gogumaUser">
	SELECT * FROM goguma_user ORDER BY uid DESC
	</select>
	
	<select id="selectOne"
		parameterType="long"
		resultType="gogumaUser">
	SELECT * FROM goguma_user WHERE uid=#{uid}
	</select>
	
	<insert id="insert" parameterType="gogumaUser">
	INSERT INTO goguma_user (name, nickname, password, telNumber, email, locaGu, locaDong, thumbnail, recommend_uid, pumpkin_point, userRate, registerDate, isDeleted)
	VALUES(#{name}, #{nickname}, #{password}, #{telNumber}, #{email}, #{locaGu}, #{locaDong}, #{thumbnail}, #{recommend_uid}, #{pumpkin_point}, #{userRate}, #{registerDate}, #{isDeleted})
	</insert>
	
	<update id="update" parameterType="gogumaUser">
	UPDATE goguma_user SET
	name=#{name}, nickname=#{nickname}, password=#{password}, telNumber=#{telNumber}, email=#{email}, locaGu=#{locaGu}, locaDong=#{locaDong}, thumbnail=#{thumbnail}, recommend_uid=#{recommend_uid}, 
	pumpkin_point=#{pumpkin_point}, userRate=#{userRate}, registerDate=#{registerDate}, isDeleted=#{isDeleted}
	WHERE uid=#{uid}
	</update>

	<delete id="delete" parameterType="int">
	DELETE FROM goguma_user WHERE uid=#{uid}
	</delete>
</mapper>